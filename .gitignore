node_modules/
tests/.tmp-*
.netlify/
.env

# Generated by `npm run build` — source of truth is src/, not these.
Mr 4x4 Auto Service-pwa/Mr 4x4 Auto Service.html
Mr 4x4 Auto Service-pwa/service-worker.js

# Android app signing identity — never share/commit (README in that folder
# explains why: it's what lets Android trust a rebuilt APK as a legitimate
# update rather than a conflicting app). Keep only on this machine.
android-app/signing.keystore
android-app/signing-key-info.txt
